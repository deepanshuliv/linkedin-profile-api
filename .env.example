# Copy to `.env` locally. Never commit real values.
# Chrome → DevTools → Network → a voyager/api request → Cookie header.

LINKEDIN_LI_AT=
LINKEDIN_JSESSIONID=

# Optional: paste the full Cookie header instead of the two values above.
# LINKEDIN_COOKIE=

PORT=3000
